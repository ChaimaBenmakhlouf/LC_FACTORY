{% extends 'base.html.twig' %}

{% block title %}CompanyInformation{% endblock %}

{% block body %}
    <h1>CompanyInformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company_information.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company_information.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ company_information.address }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ company_information.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('company_information_index') }}">back to list</a>

    <a href="{{ path('company_information_edit', {'id': company_information.id}) }}">edit</a>

    {{ include('company_information/_delete_form.html.twig') }}
{% endblock %}
